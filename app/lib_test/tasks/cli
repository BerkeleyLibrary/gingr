require 'thor'

# test class
class MyCLI < Thor
  desc 'hello NAME', 'say hello to NAME'
  option :from
  def hello(name)
    puts "from: #{options[:from]}" if options[:from]
    puts "Hello #{name}"
  end

  def self.exit_on_failure?
    true
  end
end
MyCLI.start(ARGV)
